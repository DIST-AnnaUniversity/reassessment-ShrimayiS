# For the enclosed dataset, use  SVM with RBF and print the accuracy (10).
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score, confusion_matrix
from sklearn.svm import SVC


# Importing Data file



#replace special character with median


# converting the object column  to float






#split the data with 80 20 and random state as 10





# Building a Support Vector Machine on train data with rbf kernel

#print the accuracy score if the kernel is rbf and round off to 0 decimal places.

acc = 
print(acc)
